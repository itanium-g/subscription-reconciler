version: "2"

sql:
  - schema: "migrations"
    queries: "sql/queries"
    engine: "postgresql"
    gen:
      go:
        package: "gen"
        out: "internal/infrastructure/postgres/gen"
        emit_interface: true
